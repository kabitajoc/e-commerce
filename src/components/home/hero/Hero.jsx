import React from "react";
import heroImg from "../homeImages/hero.jpg";

const Hero = () => {
  return (
    <div className="hero">
      <img src={heroImg} alt="shopping site" />
    </div>
  );
};

export default Hero;
